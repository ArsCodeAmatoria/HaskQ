{
  "version": 2,
  "github": {
    "silent": true
  },
  "framework": "docusaurus-2",
  "builds": [
    { 
      "src": "apps/docs/package.json",
      "use": "@vercel/static-build",
      "config": { 
        "distDir": "apps/docs/build",
        "buildCommand": "cd apps/docs && npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/apps/docs/build/$1" }
  ]
} 